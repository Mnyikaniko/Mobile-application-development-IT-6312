package com.example.studentcrudapp.data.entity

import androidx.room.Entity
import androidx.room.PrimaryKey //key for primary import

@Entity(tableName = "students")
data class Student(
    @PrimaryKey(autoGenerate = true) val id: Int = 0,
    val name: String,
    val course: String
)
